
on: push

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build Project
        uses: Whiley/WhileyBuildAction@v2
        with:
          checking: true
